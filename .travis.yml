sudo: required
services:
  - docker

cache:
  directories:
    - docker-cache

before_install:
  - docker build -t a0987654321/redis_reader -f Dockerfile .

script:
  - docker run a0987654321/redis_reader

language: generic